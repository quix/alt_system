
Alternate implementations of system() and backticks `` for Windows.

Problems in the old implementations are detailed by the spec outputs.

  % gem install rspec

New and old spec output:

  % rake

Or individually:

  % rake new
  % rake old

